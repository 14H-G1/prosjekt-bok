/*  Goal with API:
 *  
 *      no need to require both db, authentication, passport etc,
 *
 *      having all functionality in one require()
 *
 * */

// adding, removing and editing users, books etc,
// search returning json for ajax calls